# Mosaic Studio

画像をサーバーへ送信せず、ブラウザ上だけでモザイク加工できるWebアプリです。

## 機能

- ドラッグ操作による複数範囲のモザイク加工
- モザイクの粗さ調整
- PNG / JPG形式での保存
- JPG画質の調整
- ドラッグ＆ドロップとスマートフォン操作
- 元画像の解像度を維持した書き出し

## 開発

```bash
npm install
npm run dev
```

本番用ビルドは `npm run build` で生成できます。
